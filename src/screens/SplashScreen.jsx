import React, { useEffect } from 'react';
import { View, Text } from 'react-native';

const SplashScreen = (props) => {
  useEffect(() => {}, []);

  return (
    <View>
      <Text>Soy lo primero que vas a ver!! Hooolaaaa</Text>
    </View>
  );
};

export default SplashScreen;
